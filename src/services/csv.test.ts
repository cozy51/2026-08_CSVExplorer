import { describe,expect,it } from 'vitest'; import { inferType,parseCsv } from './csv'; import { parseWithFormat } from './formatRegistry';
describe('CSV parsing',()=>{it('handles quoted commas and newlines',()=>expect(parseCsv('a,b\n"x,y","z\nq"')).toEqual([['a','b'],['x,y','z\nq']]));it('infers common types',()=>{expect(inferType(['1','2','3'])).toBe('number');expect(inferType(['ON','OFF','ON'])).toBe('boolean');expect(inferType(['0x01','0x02'])).toBe('code')});it('falls back to generic',()=>expect(parseWithFormat({fileName:'plain.csv',text:'x,y\n1,2'}).metadata.formatId).toBe('generic'));it('detects trace and derives timestamp',()=>{const d=parseWithFormat({fileName:'trc_0001.csv',text:'meta,,,\nGroup,Time,Time\nValue,時間,msec\n1,2026-01-01 00:00:00,123\n2,2026-01-01 00:00:01,456'});expect(d.metadata.formatId).toBe('trace');expect(d.metadata.xAxisId).toBe('__timestamp');expect(d.rows[0].__timestamp).toContain('.123Z')})});
