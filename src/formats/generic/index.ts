import type { CsvFormatAdapter } from '../../models/dataset';
import { defineColumns, detectDelimiter, parseCsv, toRows } from '../../services/csv';
export const genericCsvFormat: CsvFormatAdapter = { id:'generic', name:'Generic CSV', detect:()=>0.1, parse(ctx,confidence=0.1){const delimiter=detectDelimiter(ctx.text); const matrix=parseCsv(ctx.text,delimiter); if(matrix.length<1)throw new Error('CSVにデータがありません。'); const headers=matrix[0], raw=matrix.slice(1); const columns=defineColumns(headers,raw); const named=columns.find(c=>/^(date|time|timestamp|datetime|elapsed|日時|時刻|時間)$/i.test(c.name)); return {metadata:{fileName:ctx.fileName,formatId:'generic',formatName:'Generic CSV',confidence,delimiter,details:{encoding:'UTF-8',headerRow:1},xAxisId:named?.id},columns,rows:toRows(raw,columns)}; }};
