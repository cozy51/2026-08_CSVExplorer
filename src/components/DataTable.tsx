import { useState } from 'react'; import type { ParsedDataset } from '../models/dataset';
export function DataTable({dataset}:{dataset:ParsedDataset}){const[start,setStart]=useState(0),page=100;const rows=dataset.rows.slice(start,start+page);return <div className="table-wrap"><div className="table-tools"><span>{start+1}–{Math.min(start+page,dataset.rows.length)} / {dataset.rows.length}</span><button disabled={!start} onClick={()=>setStart(Math.max(0,start-page))}>前へ</button><button disabled={start+page>=dataset.rows.length} onClick={()=>setStart(start+page)}>次へ</button></div><div className="table-scroll"><table><thead><tr><th>#</th>{dataset.columns.map(c=><th key={c.id}>{c.name}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={start+i}><td>{start+i+1}</td>{dataset.columns.map(c=><td key={c.id}>{String(r[c.id]??'')}</td>)}</tr>)}</tbody></table></div></div>}
